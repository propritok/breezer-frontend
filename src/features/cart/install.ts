// Виды монтажа и скидка задаются в PocketBase (коллекции install_prices и settings),
// см. src/shared/api/settings.ts и хук useSiteSettings(). Здесь только тип кода варианта.
export type InstallId = string;
